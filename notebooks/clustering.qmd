---
title: "clustering"
format: html
editor: visual
---

```{r warning=FALSE, message=FALSE}
library(httr)
library(jsonlite)
library(dplyr)

source("../R/nba-functions.R") #add echo = T to see more details
```

```{r}
library(ggplot2)
library(gridExtra)
library(ggpubr) # sudo apt-get install liblapack-dev libblas-dev gfortran
#library(tidyverse)
library(dplyr)
library(BasketballAnalyzeR)

library(extrafont)
library(cowplot)
library(transport)
library(curveDepth)
```

```{r}
year = 2019
players <- list(
  list(name = "Bam Adebayo",         season = year),
  list(name = "James Harden",           season = year)
)

shots_by_player <- lapply(players, function(p) {
  nba_data(Player = p$name, season = p$season)
})
names(shots_by_player) <- sapply(players, function(p) p$name)

data_by_player<- lapply(shots_by_player, nba_data_depth)
```

```{r}
 shots_by_player[[1]][shots_by_player[[1]]$SHOT_MADE == T,]
```

```{r}
setwd("../data")
source("../R/nba-functions.R")
source('../R/nba_functions_database.R', chdir = TRUE)
year <- 2019

files <- c(
  "Bam_Adebayo_2018_2024.csv",           #1
  "Stephen_Curry_2015_2022.csv" ,         #2
  "Klay_Thompson_2015_2024.csv",         #3
  "Draymond_Green_2015_2024.csv",        #4
  "Kyle_Korver_2014_2020.csv",           #5
  "James_Harden_2015_2024.csv",          #6
  "Evan_Fournier_2015_2024.csv",         #7
  "Anthony_Davis_2015_2025.csv",         #8
  "Giannis_Antetokounmpo_2015_2024.csv", #9
  "Jarrett_Allen_2018_2024.csv",         #10
  "Alex_Caruso_2018_2024.csv",           #11
  "Kevin_Durant_2015_2024.csv",          #12
  "LeBron_James_2015_2024.csv",          #13
  "Joel_Embiid_2017_2022.csv"            #14
  
)
setwd("../data")
shots_by_player <- lapply(files, function(f) {
    nba_data_csv(f, year)
})
all_shots_made_by_player <- lapply(shots_by_player, function(df) {
  df[df$SHOT_MADE == TRUE, ]
})
data_by_player <- lapply(files, function(f) {
  nba_data_depth(
    nba_data_csv(f, year)
  )
})
names(data_by_player) <- gsub("_\\d{4}_\\d{4}\\.csv$", "", files)
names(data_by_player) <- gsub("_", " ", names(data_by_player))
names(all_shots_made_by_player) <- gsub("_\\d{4}_\\d{4}\\.csv$", "", files)
names(all_shots_made_by_player) <- gsub("_", " ", names(data_by_player))
```

```{r}

# Extract made-shot point patterns for every player
data_made_by_player <- lapply(data_by_player, function(joueur) {
  nba_data_depth_made(joueur)
})
names(data_made_by_player) <- names(data_by_player)

# Keep only the most typical matches according to Tukey depth
data_typic_made_all_players <- lapply(data_made_by_player, function(joueur_data) {
  prof_matchs <- depths.Tukey(
    joueur_data,
    joueur_data,
    nDirs = 250,
    subs = FALSE,
    exactEst = FALSE,
    parConst1 = -2,
    parConst2 = 5
  )
  seuil_matchs <- quantile(prof_matchs, probs = 0.5, na.rm = TRUE)
  joueur_filtre <- joueur_data[prof_matchs >= seuil_matchs]
  return(joueur_filtre)
})

names(data_typic_made_all_players) <- names(data_made_by_player)
```

```{r}
shots_made_by_player <- lapply(data_typic_made_all_players, function(joueur_list) {
  joueur_df <- do.call(rbind, lapply(names(joueur_list), function(id) {
    match <- joueur_list[[id]]
    data.frame(
      idGame    = id,
      dateGame  = match$date,
      LOC_X     = match$coords[, 1],
      LOC_Y     = match$coords[, 2],
      SHOT_MADE = TRUE
    )
  }))
  return(joueur_df)
})

names(shots_made_by_player) <- names(data_typic_made_all_players)
```

```{r}
library(patchwork)
p01 <- plot_density_court(shots_made_by_player[[10]] , sigma = 4)
p02 <- plot_density_court(shots_made_by_player[[12]] , sigma = 4)

(p01 + p02)
```

```{r}
library(patchwork)

p1 <- plot_season2(shots_made_by_player[[12]])
p2 <- plot_season2(shots_made_by_player[[10]])

(p1 + p2)

```

## Distances entre processus

## distance entre kde

```{r}
library(spatstat)

evaluer_distance <- function(data_joueur_A, data_joueur_B, sigma = 3, fenetre = owin(c(-25, 25), c(-47.1, 0)) ) {
  
  ppp_A <- ppp(x = data_joueur_A$LOC_X, y = data_joueur_A$LOC_Y, window = fenetre)
  ppp_B <- ppp(x = data_joueur_B$LOC_X, y = data_joueur_B$LOC_Y, window = fenetre)
  
  kde_A <- density(ppp_A, sigma = sigma, at = "pixels")
  kde_B <- density(ppp_B, sigma = sigma, at = "pixels", dimyx = dim(kde_A)) 
  
  distance_KDE <- integral((kde_A - kde_B)^2)
  
  return(distance_KDE)
}

pl_1 <- shots_made_by_player[[1]]
pl_2 <- shots_made_by_player[[2]]
evaluer_distance(pl_2, pl_1)
```

## distance baddeley

```{r}
library(spatstat)

evaluer_distance <- function(data_joueur_A, data_joueur_B, c_cutoff = 5, fenetre = owin(c(-25, 25), c(-47.1, 0))) {
  
  # 1. Création des objets ppp pour les deux joueurs
  ppp_A <- ppp(x = data_joueur_A$LOC_X, y = data_joueur_A$LOC_Y, window = fenetre)
  ppp_B <- ppp(x = data_joueur_B$LOC_X, y = data_joueur_B$LOC_Y, window = fenetre)
  
  # 2. Calcul de la distance Delta de Baddeley
  # p = 1 correspond à l'exposant de l'intégrale externe
  distance_Badd <- deltametric(ppp_A, ppp_B, p = 1, c = c_cutoff)
  
  return(distance_Badd)
}

# --- Test de la fonction ---
pl_1 <- shots_made_by_player[[1]]
pl_2 <- shots_made_by_player[[2]]

evaluer_distance(pl_2, pl_1, c_cutoff = 5)
```

## Distance entre match kde

```{r}
library(spatstat)

evaluer_distance_matchs <- function(match_A, match_B, sigma = 3, fenetre_terrain = owin(c(-25, 25), c(-47.1, 0))) {
  
  # Extraction des matrices de coordonnées [,1] = X, [,2] = Y
  coords_A <- match_A$coords
  coords_B <- match_B$coords
  
  # Création des objets ppp pour spatstat
  ppp_A <- ppp(x = coords_A[, 1], y = coords_A[, 2], window = fenetre_terrain)
  ppp_B <- ppp(x = coords_B[, 1], y = coords_B[, 2], window = fenetre_terrain)
  
  # Calcul des densités de kernel (KDE)
  kde_A <- density(ppp_A, sigma = sigma, at = "pixels")
  kde_B <- density(ppp_B, sigma = sigma, at = "pixels", dimyx = dim(kde_A)) 
  
  # Calcul et retour de la distance L2 (Intégration de la différence au carré)
  return(integral((kde_A - kde_B)^2))
}
```

## distance entre match baddeley

```{r}
library(spatstat)

evaluer_distance_matchs <- function(match_A, match_B, c_cutoff = 5, fenetre_terrain = owin(c(-25, 25), c(-47.1, 0))) {
  coords_A <- match_A$coords
  coords_B <- match_B$coords
  ppp_A <- ppp(x = coords_A[, 1], y = coords_A[, 2], window = fenetre_terrain)
  ppp_B <- ppp(x = coords_B[, 1], y = coords_B[, 2], window = fenetre_terrain)
  
  # p = 1 correspond à une différence absolue moyenne sur la grille de pixels
  # c_cutoff est le seuil de saturation (en unités du terrain, ex: pieds)
  distance_baddeley <- deltametric(ppp_A, ppp_B, p = 1, c = c_cutoff)
  return(distance_baddeley)
}
```

```{r}
n <- length(shots_made_by_player)
D <- matrix(0, n, n)

for(i in 1:n){
  for(j in i:n){
    
    # Utilisation directe de ta fonction sur les dataframes d'origine
    dij <- evaluer_distance(
      shots_made_by_player[[i]],
      shots_made_by_player[[j]],
      #sigma = 3
    )
    
    D[i, j] <- dij
    D[j, i] <- dij
  }
}

rownames(D) <- names(data_by_player)
colnames(D) <- names(data_by_player)

# Affichage de la matrice et du Heatmap
D
heatmap(D)
```

#### Estimation de l'intensité

##### Moyennes des intensités des matchs de joueurs

```{r}
library(spatstat)
fenetre_terrain <- owin(c(-25, 25), c(-47.1, 0))
sigma_kde <- 2 
# ==============================================================================
# PIPELINE : DENSITÉ MOYENNE ET MATCH LE PLUS PROFOND
# ==============================================================================

analyse_joueurs <- lapply(names(data_typic_made_all_players), function(nom_joueur) {
  
  joueur_list <- data_typic_made_all_players[[nom_joueur]]
  match_ids <- names(joueur_list)
  list_kde <- lapply(match_ids, function(id) {
    match <- joueur_list[[id]]
    coords <- match$coords
    
    ppp_match <- ppp(x = coords[, 1], y = coords[, 2], window = fenetre_terrain)
    kde <- density(ppp_match, sigma = sigma_kde, at = "pixels", dimyx = c(128, 128))
    return(kde)
  })
  names(list_kde) <- match_ids
  
  matrices_v <- lapply(list_kde, function(img) img$v)
  matrice_moyenne <- Introduce <- Reduce("+", matrices_v) / length(matrices_v)
  
  kde_moyen <- im(matrice_moyenne, xcol = list_kde[[1]]$xcol, yrow = list_kde[[1]]$yrow)
  
  
  # --- Retour de l'objet structuré ---
  return(list(
    player_name   = nom_joueur,
    kde_mean      = kde_moyen,
    #deepest_match = list(id = id_deepest, kde = kde_deepest, depth_value = max(profondeurs)),
    all_kdes      = list_kde
  ))
})

names(analyse_joueurs) <- names(data_typic_made_all_players)
```

```{r}
noms_joueurs <- names(analyse_joueurs)
vecteurs_densite_moyenne <- do.call(rbind, lapply(noms_joueurs, function(nom) {
  kde_obj <- analyse_joueurs[[nom]]$kde_mean
  as.vector(kde_obj$v) 
}))

rownames(vecteurs_densite_moyenne) <- noms_joueurs

matrice_distance_moyenne <- dist(vecteurs_densite_moyenne, method = "euclidean")
matrice_dist_complete_moyenne <- as.matrix(matrice_distance_moyenne)

# Renommer en D pour tes scripts de clustering
D <- matrice_dist_complete_moyenne
heatmap(matrice_dist_complete_moyenne)
```

#### Intensité de la plus profonde

```{r}
set.seed(42)
library(spatstat)
estimate_single_deepest_density <- function(data_joueurs, window = owin(c(-25, 25), c(-47.1, 0)), sigma = 2) {
  
  resultats <- lapply(names(data_joueurs), function(nom_joueur) {
    joueur_data <- data_joueurs[[nom_joueur]]
    match_ids <- names(joueur_data)
    prof_matchs <- depths.Tukey(joueur_data, joueur_data, nDirs = 250, subs = FALSE, exactEst = FALSE, parConst1 = -2,
                                parConst2 = 5)
    names(prof_matchs) <- match_ids
    id_match_typique <- match_ids[which.max(prof_matchs)]
    le_match_typique <- joueur_data[[id_match_typique]]
    coords <- le_match_typique$coords
    
    # 3. Estimation de la densité KDE de CE match unique
    ppp_match <- ppp(x = coords[, 1], y = coords[, 2], window = window)
    kde_match_typique <- density(ppp_match, sigma = sigma, at = "pixels", dimyx = c(128, 128))
    
    return(list(
      player_name   = nom_joueur,
      match_id      = id_match_typique,
      kde_typical   = kde_match_typique
    ))
  })
  
  names(resultats) <- names(data_joueurs)
  return(resultats)
}
```

```{r}
densites_match_type <- estimate_single_deepest_density(data_joueurs = data_made_by_player, window = fenetre_terrain,
  sigma = 2
)
```

```{r}
noms_joueurs <- names(densites_match_type)
vecteurs_densite <- do.call(rbind, lapply(noms_joueurs, function(nom) {
  kde_obj <- densites_match_type[[nom]]$kde_typical
  as.vector(kde_obj$v) # Transforme la matrice 128x128 en un vecteur de 16384 valeurs
}))

rownames(vecteurs_densite) <- noms_joueurs

# ==============================================================================
# 2. CALCUL DE LA MATRICE DE DISTANCE L2 (EUCLIDIENNE)
# ==============================================================================
matrice_distance <- dist(vecteurs_densite, method = "euclidean")
matrice_dist_complete <- as.matrix(matrice_distance)
D <- matrice_dist_complete
heatmap(matrice_dist_complete)
```

```{r}
mon_theme<- theme(
  panel.background = element_rect(fill = "white", colour = NA),
  panel.border     = element_rect(fill = NA, colour = "black"),
  panel.grid       = element_line(colour = "grey70", linetype = "dotted", linewidth = 0.3),
  panel.grid.minor = element_line(linewidth = rel(1)),
  legend.key       = element_rect(fill = "white", colour = NA)
)
```

```{r}
#UMAP
set.seed(42)
# install.packages("umap")
library(umap)
library(ggplot2)
library(ggrepel)

# 1. Configuration de l'UMAP pour une matrice de distance
configuration_umap <- umap.defaults
configuration_umap$input <- "dist"
configuration_umap$n_neighbors <- min(5, nrow(D) - 1) # À ajuster selon ton nombre de joueurs

# 2. Calcul de la réduction de dimension
res_umap <- umap(D, config = configuration_umap)

# 3. Création du Dataframe pour le graphique
umap_df <- data.frame(
  Dim1 = res_umap$layout[, 1],
  Dim2 = res_umap$layout[, 2],
  Joueur = rownames(D)
)

# 4. Affichage moderne avec ggplot2
ggplot(umap_df, aes(x = Dim1, y = Dim2, label = Joueur)) +
  geom_point(color = "darkviolet", size = 3, alpha = 0.8) +
  geom_text_repel(size = 2.5, max.overlaps = 15) + #coord_fixed() +
  labs(
    title = "Cartographie UMAP des Joueurs NBA",
    x = "UMAP Dimension 1",
    y = "UMAP Dimension 2"
  ) + mon_theme
```

```{r}
#ISOMAP
#install.packages("vegan")
library(vegan)
library(ggplot2)
library(ggrepel)

res_isomap <- isomap(D, ndim = 2, k = 2)

# 2. Extraction des coordonnées pour ggplot
isomap_df <- data.frame(
  Dim1 = res_isomap$points[, 1],
  Dim2 = res_isomap$points[, 2],
  Joueur = rownames(D)
)

# 3. Affichage avec ton thème personnalisé (fond blanc, pointillés fins)
ggplot(isomap_df, aes(x = Dim1, y = Dim2, label = Joueur)) +
  geom_point(color = "darkviolet", size = 1.5, alpha = 0.8) + #coord_fixed(ratio = 1) +
  geom_text_repel(size = 3.5, max.overlaps = 15) +
  labs(
    title = "Cartographie Isomap des Joueurs NBA",
    x = "Isomap Dimension 1",
    y = "Isomap Dimension 2"
  ) +
  mon_theme
```

```{r}
library(ggplot2)
library(ggrepel)

mds_classique <- cmdscale(D, k = 2, eig = TRUE)
set.seed(42)
# Extraction des coordonnées 2D
mds_df <- data.frame(
  Dim1 = mds_classique$points[, 1],
  Dim2 = mds_classique$points[, 2],
  Joueur = rownames(D)
)

# 2. Affichage avec ton thème personnalisé
ggplot(mds_df, aes(x = Dim1, y = Dim2, label = Joueur)) +
  geom_point(color = "darkviolet", size = 2, alpha = 0.8) +
  geom_text_repel(size = 2.5, max.overlaps = 15) +
  coord_fixed(ratio = 1) + # CRUCIAL pour le MDS : conserve l'échelle géométrique 1:1
  labs(
    title = "Cartographie MDS Classique ", x = "Axe Principal 1",    y = "Axe Principal 2"
  ) + mon_theme
```

```{r}
# Classification Ascendante Hiérarchique (méthode de Ward)
cah_core <- hclust(as.dist(D), method = "ward.D2")
plot(cah_core, main = "Dendrogramme des joueurs", xlab = "Joueurs", sub = "")

# Découpage en 3 clusters
cluster_assignments <- cutree(cah_core, k = 3)

```

```{r}
set.seed(42)
proj_mds <- cmdscale(D, k = 2)
plot_df <- data.frame(
  Dim1    = proj_mds[, 1],
  Dim2    = proj_mds[, 2],
  Match   = rownames(D),
  Cluster = as.factor(cluster_assignments)
)

# Graphique final des profils types du joueur
ggplot(plot_df, aes(x = Dim1, y = Dim2, color = Cluster, label = Match)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_text_repel(size = 3, max.overlaps = 15, show.legend = FALSE) + coord_fixed(ratio = 1) +
  labs(
    title = "Typologie des joueurs",
    x = "Dimension 1",
    y = "Dimension  2"
  ) +
  scale_color_brewer(palette = "Set1") + mon_theme
```

```{r}
library(cluster)
sil <- silhouette(cluster_assignments,
                  as.dist(D))
mean(sil[,3])
```

## Matrice de distance entre match d'un joueur

```{r}
matchs_joueur <- nba_data_depth_made(data_by_player[[9]])
#matchs_joueur <- data_by_player[[14]]

# 2. Initialisation de la matrice de distance pour ce joueur
n_matchs <- length(matchs_joueur)
noms_matchs <- names(matchs_joueur) 

D_matchs <- matrix(0, nrow = n_matchs, ncol = n_matchs)
rownames(D_matchs) <- noms_matchs
colnames(D_matchs) <- noms_matchs

for (i in 1:(n_matchs - 1)) {
  for (j in (i + 1):n_matchs) {
    match_i <- matchs_joueur[[i]]
    match_j <- matchs_joueur[[j]]
    dij <- evaluer_distance_matchs(match_i, match_j)
    D_matchs[i, j] <- dij
    D_matchs[j, i] <- dij
  }
}
```

```{r}
i = 4; j = 7;

pl_1 <- matchs_joueur[[i]]
pl_2 <- matchs_joueur[[j]]
print(evaluer_distance_matchs(pl_1, pl_2))

p11 <- plot_match2(matchs_joueur, idMatch = attributes(matchs_joueur)$names[i])
p12 <- plot_match2(matchs_joueur, idMatch = attributes(matchs_joueur)$names[j])
(p11 + p12)

```

## Calcul du voisinage d'un match

```{r}
k <- 20
neighbors <- vector("list", n_matchs)
for(i in 1:n_matchs){
  d_i <- D_matchs[i, ]
  ord <- order(d_i)
  ord <- ord[ord != i]
  neighbors[[i]] <- ord[1:k]
}
```

```{r}
neighbors[[1]]
```

## Calcul de la profondeur dans le voisinage

```{r}
library(curveDepth)
set.seed(42)
LD <- numeric(n_matchs)
i <- 1

for(i in 1:n_matchs){
  
  voisinage <- matchs_joueur[c(i, neighbors[[i]])]
  LD[i] <- depths.Tukey(matchs_joueur[i], voisinage, nDirs = 250, subs = FALSE, exactEst = FALSE,  parConst1 = -2, parConst2 = 5 )
  
}
LD
```

```{r}
hist(LD)
```

## Noyau locaux : core

```{r}
q <- 0.25
alpha_q <- quantile(LD, probs = 1-q)
core_idx <- which(LD > alpha_q)
core_matches <- matchs_joueur[core_idx]
print(alpha_q)
attributes(core_matches)
```

```{r}
# Extraction de la sous-matrice de distance pour le core
D_core <- D_matchs[core_idx, core_idx]
D_core_dist <- as.dist(D_core)
```

```{r}
set.seed(42)
proj_mds <- cmdscale(D_core, k = 2)
df <- data.frame(
  Dim1    = proj_mds[, 1],
  Dim2    = proj_mds[, 2],
  Joueur = rownames(D_core)
)

ggplot(df, aes(x = Dim1, y = Dim2, label = Joueur)) +
  geom_point(color = "darkviolet", size = 2, alpha = 0.8) + mon_theme +
  geom_text_repel(size = 2, max.overlaps = 15) + #coord_fixed(ratio = 1) + 
  labs(title = "Cartographie MDS Classique ", x = "Axe Principal 1",    y = "Axe Principal 2") 
```

## Clustering sur les core

```{r}
library(cluster)

nb_clusters <- 2
res_pam <- pam(D_core_dist, k = nb_clusters, diss = TRUE)

#heatmap(D_core)
cluster_assignments <- res_pam$clustering
table(cluster_assignments)
```

```{r}
  # Classification Ascendante Hiérarchique
  cah_core <- hclust(D_core_dist, method = "ward.D2")
  plot(cah_core, main = "Dendrogramme des Matchs du Core", xlab = "Matchs", sub = "")
  # Découpage en 3 clusters
  cluster_assignments <- cutree(cah_core, k = 2)
```

```{r}
set.seed(42)
configuration_umap <- umap.defaults
configuration_umap$input <- "dist"
configuration_umap$n_neighbors <- min(6,nrow(D_core)-1)

proj  <- umap(D_core, config = configuration_umap)
plot_df <- data.frame(
  Dim1 = proj$layout[, 1],
  Dim2 = proj$layout[, 2],
  Match = rownames(D_core),
  Cluster = as.factor(cluster_assignments)
)

```

```{r}
proj_mds <- cmdscale(D_core, k = 2)
plot_df <- data.frame(
  Dim1    = proj_mds[, 1],
  Dim2    = proj_mds[, 2],
  Match   = rownames(D_core),
  Cluster = as.factor(cluster_assignments)
)

ggplot(plot_df, aes(x = Dim1, y = Dim2, color = Cluster, label = Match)) +
  geom_point(size = 2) +
  geom_text_repel(size = 3, max.overlaps = 10, show.legend = FALSE) + mon_theme + coord_fixed(ratio = 1) + 
  labs(
    title = "Typologie des Matchs au sein du Core",
    x = "Dimension MDS 1",
    y = "Dimension MDS 2"
  ) + scale_color_brewer(palette = "Set1") 
```

## Partition finale

```{r}
library(cluster)

medoides_core_names <- res_pam$medoids
medoides_global_idx <- which(rownames(D_matchs) %in% medoides_core_names)

partition_finale <- numeric(n_matchs)
names(partition_finale) <- rownames(D_matchs)

for (i in 1:n_matchs) {
  distances_aux_centres <- D_matchs[i, medoides_global_idx]
  partition_finale[i] <- which.min(distances_aux_centres)
}

table(partition_finale)
```

```{r}
partition_finale <- numeric(n_matchs)
names(partition_finale) <- rownames(D_matchs)
k_clusters <- max(cluster_assignments)

for (i in 1:n_matchs) {
  dist_to_clusters <- numeric(k_clusters)
  for (j in 1:k_clusters) {
    matchs_du_cluster_j <- names(cluster_assignments)[cluster_assignments == j]
    distances_cibles <- D_matchs[i, matchs_du_cluster_j]
    dist_to_clusters[j] <- min(distances_cibles)
  }
  partition_finale[i] <- which.min(dist_to_clusters)
}
table(partition_finale)
```

```{r}
set.seed(42)

proj_mds <- cmdscale(D_matchs, k = 2)
plot_global_df<- data.frame(
  Dim1    = proj_mds[, 1],
  Dim2    = proj_mds[, 2],
  Match = rownames(D_matchs),
  Cluster = as.factor(partition_finale)
)

ggplot(plot_global_df, aes(x = Dim1, y = Dim2, color = Cluster, label = Match)) +
  geom_point(size = 2.5, alpha = 0.8) +
  geom_text_repel(size = 2, max.overlaps = 10, show.legend = FALSE) +
  scale_shape_manual(values = c(16, 17)) + #  coord_fixed() +
  labs(
    title = "Partition Finale de l'ensemble des Matchs du Joueur",
    x = "Dimension 1",
    y = "Dimension 2"
  ) +
  scale_color_brewer(palette = "Set1") + mon_theme
```

```{r}

figure <- depths.Tukey(matchs_joueur, matchs_joueur, nDirs = 250, subs = FALSE, exactEst = FALSE,  parConst1 = -2, parConst2 = 5 )

deepesty <- order(figure)

noms_matchs_pures <- names(matchs_joueur)
noms_matchs_ordonnes <- noms_matchs_pures[deepesty]

clusters_ordonnes <- as.factor(partition_finale[noms_matchs_ordonnes])

df_plot <- data.frame(
  x = seq_along(figure),
  y = figure[deepesty],
  Cluster = clusters_ordonnes
)

p5 <- ggplot(df_plot, aes(x = x, y = y, color = Cluster)) +
  geom_segment(aes(xend = x, yend = 0), linewidth = 0.7) +
  xlab("Matchs ordonnés par profondeur") +
  ylab("Profondeur de Tukey") +
  ggtitle("Profondeurs des matchs colorées par Cluster") +
  scale_color_brewer(palette = "Set1") + mon_theme

p5
```

## DDclust

```{r}

K <- length(unique(partition_finale))
clusters <- vector("list", K)
for(k in 1:K){
  ids <- names(partition_finale)[partition_finale == k]
  clusters[[k]] <- matchs_joueur[ids]
}
#=========================================================
Depth_within <- numeric(length(matchs_joueur))
names(Depth_within) <- names(matchs_joueur)

for(i in seq_along(matchs_joueur)){
  id <- names(matchs_joueur)[i]
  k  <- partition_finale[id]
  Depth_within[i] <- depths.Tukey(
    list(matchs_joueur[[i]]),
    clusters[[k]],
    nDirs = 250,
    subs = FALSE,
    exactEst = FALSE,
    parConst1 = -2,
    parConst2 = 5
  )
}

#===================================================================
Depth_other <- numeric(length(matchs_joueur))
names(Depth_other) <- names(matchs_joueur)

for(i in seq_along(matchs_joueur)){
  
  id <- names(matchs_joueur)[i]
  k  <- partition_finale[id]
  
  other_depths <- c()
  
  for(j in setdiff(1:K, k)){
    
    d <- depths.Tukey(
      list(matchs_joueur[[i]]),
      clusters[[j]],
      nDirs = 250,
      subs = FALSE,
      exactEst = FALSE,
      parConst1 = -2,
      parConst2 = 5
    )
    other_depths <- c(other_depths, d)
  }
  Depth_other[i] <- min(other_depths)
}
#===================================================
RelativeDepth <- Depth_within - Depth_other

#==================================================
results <- data.frame(
  Match = names(matchs_joueur),
  Cluster = partition_finale,
  Depth_within = Depth_within,
  Depth_other = Depth_other,
  RelativeDepth = RelativeDepth
)

library(dplyr)
results %>%
  group_by(Cluster) %>%
  arrange(desc(RelativeDepth), .by_group = TRUE)
```

```{r}
sil <- silhouette(partition_finale,
                  as.dist(D_matchs))
mean(sil[,3])
```

```{r}

p11 <- plot_match2(matchs_joueur, idMatch = "21800870")
p12 <- plot_match2(matchs_joueur, idMatch = "21801043")
(p11 + p12 )

```

```{r}
library(patchwork)

clusters <- sort(unique(partition_finale))
plots_clusters <- lapply(clusters, function(cl){
  
  matchs_cluster <- matchs_joueur[names(partition_finale)[partition_finale == cl]]
  shots_cluster <- do.call(
    rbind,
    lapply(names(matchs_cluster), function(id){
      
      match <- matchs_cluster[[id]]
      
      data.frame(
        idGame    = id,
        dateGame  = match$date,
        LOC_X     = match$coords[,1],
        LOC_Y     = match$coords[,2],
        SHOT_MADE = TRUE
      )
    })
  )
  
  ## Plot
  plot_season(shots_cluster) +
    ggtitle(
      paste0(
        "Cluster ", cl,
        "\n",
        length(matchs_cluster), " matches"
      )
    )
})

wrap_plots(plots_clusters, ncol =2)
```

```{r}
# --- extraction des processus par cluster ------------------------------------
cluster_1 <- matchs_joueur[names(partition_finale)[partition_finale == 1]]
cluster_2 <- matchs_joueur[names(partition_finale)[partition_finale == 2]]

# --- DD-plot entre les deux clusters -----------------------------------------
figure_clusters <- ddplot_nba(cluster_1, cluster_2, Ndirs = 250,
                              parConst1 = -2, parConst2 = 5)
```
