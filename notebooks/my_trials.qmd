---
title: "NBA API trials"
format: html
editor: visual
---

```{r warning=FALSE, message=FALSE}
setwd("..")
library(httr)
library(jsonlite)
library(dplyr)

source("R/nba-functions.R", echo = T)
```

## Test get_player_id

```{r}
id <- get_player_id("Giannis Antetokounmpo")
print(id)
```

## Test shot retrieval (hardcoded player ID)

```{r}
# LeBron James player ID = 2544 (hardcoded to bypass get_player_id)
player_id <- id
season_str <- "2018-19"

resp <- httr::GET(
  "https://stats.nba.com/stats/shotchartdetail",
  query = list(
    PlayerID       = player_id,
    Season         = season_str,
    SeasonType     = "Regular Season",
    TeamID         = 0, GameID        = "",
    ContextMeasure = "FGA",
    DateFrom       = "", DateTo        = "",
    GameSegment    = "", LastNGames    = 0,
    LeagueID       = "00", Location   = "",
    Month          = 0, OpponentTeamID = 0,
    Outcome        = "", Period        = 0,
    Position       = "", RookieYear    = "",
    SeasonSegment  = "", VsConference  = "",
    VsDivision     = ""
  ),
  httr::add_headers(
    `User-Agent`         = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
    `Referer`            = "https://www.nba.com/",
    `Origin`             = "https://www.nba.com",
    `x-nba-stats-origin` = "stats",
    `x-nba-stats-token`  = "true",
    `Accept`             = "application/json, text/plain, */*",
    `Accept-Language`    = "en-US,en;q=0.9",
    `Connection`         = "keep-alive"
  ),
  httr::timeout(30)
)

cat("HTTP status:", httr::status_code(resp), "\n")
```

```{r}
# parse only if request succeeded
parsed <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
hdrs   <- parsed$resultSets$headers[[1]]
rows   <- parsed$resultSets$rowSet[[1]]
raw    <- as.data.frame(rows, stringsAsFactors = FALSE)
colnames(raw) <- hdrs
cat("Rows retrieved:", nrow(raw), "\n")
head(raw[, c("PLAYER_NAME", "GAME_ID", "GAME_DATE", "LOC_X", "LOC_Y", "SHOT_MADE_FLAG")])
```

## Test nba_data

```{r}
shots <- nba_data(Player = "Giannis Antetokounmpo", season = 2022)
#str(shots)
head(shots)
```

```{r}
library(ggplot2)
library(gridExtra)
library(ggpubr) # sudo apt-get install liblapack-dev libblas-dev gfortran
library(tidyverse)
library(dplyr)
library(BasketballAnalyzeR)

library(extrafont)
library(cowplot)
library(transport)
```

```{r}
# source("R/nba-functionsbis.R", echo = T)
#plot_season(shots)
nba_data_depth(shots)[1]
```
