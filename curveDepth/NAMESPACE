useDynLib(curveDepth)
export(voxelize, dist.images, dist.curves, dist.curves.asymm,
       depth.curve.Tukey, sample.curves, images2curves, depthc.Tukey, 
       sample.sets, depths.Tukey, sample.pointsSets)
import(Rcpp)
import(ddalpha)
